from setuptools import find_packages, setup

setup(
    name="network_routing",
    packages=find_packages(),
    version="0.0.1",
    description="ETL tools to prepare input data for the VisionEval model",
    author="DVRPC",
)